import { styled } from '@stitches/react';

export const AsideItem = styled('div', {
  width: '120px',
  height: 'auto',
  color: '#fff',
  border: '2px solid #fff',
  marginBottom: '5px',
  borderRadius: '5px',
  textAlign: 'center',
  '&:hover': {
    cursor: 'pointer',
  },
});
